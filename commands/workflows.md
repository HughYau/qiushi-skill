---
name: workflows
description: |
  选择并执行一条标准化的多-skill 工作流。
---

读取并遵循 `skills/workflows/SKILL.md`。

任务：
$ARGUMENTS

输出要求：
1. 在四种标准组合模式中选出最匹配的一种。
2. 明确每一步的交接物和终止条件；不需要的步骤可以跳过，但要说明为什么。
3. 如果任务不适合组合模式，说明应改用哪个单独 skill。
